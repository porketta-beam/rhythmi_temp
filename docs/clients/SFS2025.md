# SFS2025 케이스 스터디

**고객사**: SFS2025 (스마트 미래사회 컨퍼런스)
**도메인**: AI/기술 컨퍼런스 경품추첨 서비스
**구현 기간**: 2025-11 ~ 2025-12
**상태**: 🔄 개발 중 (Phase 1)
**타겟 디바이스**: 대형 스크린 (프로젝터/대형 TV, 16:9 가로형)

---

## 📋 프로젝트 개요

### 고객사 프로필

**SFS2025 (스마트 미래사회 컨퍼런스)**는 과학기술정보통신부와 KIDIS가 주최하는 AI 기술 컨퍼런스입니다.

**행사 정보**:
- **일시**: 2025년 12월 6일(토) 09:00~18:00
- **장소**: 국회의사당 의원회관 대회의실
- **테마**: 기술 발전이 재편하는 국가, 기업, 개인의 권력과 역할

**비즈니스 모델**:
- 컨퍼런스 경품추첨 이벤트
- 참여자 실시간 응모
- 공정한 추첨 애니메이션 시연

**타겟 사용자**:
- 컨퍼런스 참석자 (약 200-500명)
- 행사 진행자/MC
- 이벤트 관리자

---

## 🎯 eventManager 활용

### 서비스명: LuckyDraw

**LuckyDraw**는 SFS2025 경품추첨 이벤트를 위한 전용 서비스입니다.

### 활용 기능

1. **회원 목록 관리**: UUID 기반 참여자 식별 (행운의 번호 발급)
2. **QR 코드 생성**: 참여자 응모 페이지 연결
3. **실시간 동기화**: 응모 현황 실시간 반영
4. **추첨 애니메이션**: 4가지 스타일의 추첨 시각화

### 페이지 구조

| 페이지 | 경로 | 설명 | 대상 |
|--------|------|------|------|
| page1 | `/luckydraw/qr` | QR코드 표시 화면 | 대형 스크린 |
| page2 | `/luckydraw/entry` | 응모 페이지 | 참여자 모바일 |
| page2-pending | `/luckydraw/entry/pending` | 대기 화면 (행운의 번호) | 참여자 모바일 |
| page2-win | `/luckydraw/entry/win` | 당첨 화면 | 당첨자 모바일 |
| page3 | `/luckydraw/draw` | 추첨 애니메이션 | 대형 스크린 |

---

## 🎨 디자인 시스템

### 컬러 팔레트

SFS2025 공식 홍보물에서 추출한 컬러 시스템입니다.

#### 메인 컬러 (배경)

```css
:root {
  /* Deep Space Background */
  --bg-deep: #0B1026;      /* 최상단/최하단 배경 */
  --bg-dark: #141B33;      /* 중간 배경 */
  --bg-navy: #1E2A4A;      /* 밝은 배경 영역 */
}
```

| 컬러 | HEX | 용도 |
|------|-----|------|
| Deep Navy | `#0B1026` | 배경 최상단/최하단, 기본 배경 |
| Dark Navy | `#141B33` | 배경 중간 영역, 카드 배경 |
| Navy Blue | `#1E2A4A` | 배경 밝은 부분, 호버 상태 |

#### 서브 컬러 (그래픽/효과)

```css
:root {
  /* Accent Colors */
  --accent-cyan: #00D4FF;    /* 빛나는 효과, 노드 연결선 */
  --accent-mint: #00FFCC;    /* 빛기둥, 글로우 효과 */
  --accent-blue: #4A90D9;    /* 그래픽 요소, 파티클 */
}
```

| 컬러 | HEX | 용도 |
|------|-----|------|
| Cyan | `#00D4FF` | 빛나는 효과, 노드 연결선, 라벨 테두리 |
| Mint | `#00FFCC` | 빛기둥, 글로우 효과, 성공 상태 |
| Sky Blue | `#4A90D9` | 그래픽 요소, 파티클, 보조 효과 |

#### 하이라이트 컬러 (강조)

```css
:root {
  /* Highlight Colors */
  --highlight-magenta: #FF00FF;  /* 핵심 강조, 당첨 효과 */
  --highlight-pink: #FF66B2;     /* 부드러운 강조 */
}
```

| 컬러 | HEX | 용도 |
|------|-----|------|
| Magenta | `#FF00FF` | 핵심 강조, 당첨 효과, CTA 버튼 |
| Pink | `#FF66B2` | 부드러운 강조, 서브 CTA |

#### 텍스트 컬러

```css
:root {
  /* Text Colors */
  --text-primary: #FFFFFF;    /* 메인 타이틀 */
  --text-secondary: #B0B8C8;  /* 서브 텍스트 */
  --text-muted: #7A8599;      /* 부가 정보 */
}
```

| 컬러 | HEX | 용도 |
|------|-----|------|
| White | `#FFFFFF` | 메인 타이틀, 핵심 텍스트 |
| Light Gray | `#B0B8C8` | 서브 텍스트, 설명 |
| Gray | `#7A8599` | 부가 정보, 비활성 상태 |

### CSS 변수 전체 정의

```css
:root {
  /* 메인 컬러 (배경) */
  --bg-deep: #0B1026;
  --bg-dark: #141B33;
  --bg-navy: #1E2A4A;

  /* 서브 컬러 (그래픽/효과) */
  --accent-cyan: #00D4FF;
  --accent-mint: #00FFCC;
  --accent-blue: #4A90D9;

  /* 하이라이트 (강조) */
  --highlight-magenta: #FF00FF;
  --highlight-pink: #FF66B2;

  /* 텍스트 */
  --text-primary: #FFFFFF;
  --text-secondary: #B0B8C8;
  --text-muted: #7A8599;

  /* 효과 */
  --glow-cyan: rgba(0, 212, 255, 0.5);
  --glow-magenta: rgba(255, 0, 255, 0.5);

  /* 반투명 */
  --overlay-dark: rgba(11, 16, 38, 0.8);
  --overlay-light: rgba(255, 255, 255, 0.1);
}
```

### Tailwind CSS 설정

```javascript
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      colors: {
        sfs: {
          // 배경
          'deep': '#0B1026',
          'dark': '#141B33',
          'navy': '#1E2A4A',
          // 서브
          'cyan': '#00D4FF',
          'mint': '#00FFCC',
          'blue': '#4A90D9',
          // 하이라이트
          'magenta': '#FF00FF',
          'pink': '#FF66B2',
        }
      }
    }
  }
}
```

---

### 비주얼 스타일

#### 배경 스타일

**1. 그라데이션 배경**
```css
.bg-sfs-gradient {
  background: linear-gradient(
    180deg,
    #1E2A4A 0%,
    #141B33 30%,
    #0B1026 100%
  );
}
```

**2. 파티클 배경**
- 우주/별 느낌의 작은 점들
- 불규칙 분포, 미세한 반짝임 애니메이션
- Canvas 또는 CSS로 구현

**3. 네트워크 그래픽**
- 노드 + 연결선 구조
- AI/테크 느낌의 시각적 요소
- Canvas/SVG로 구현

#### 글로우 효과

**Cyan 글로우 (노드, 연결선)**
```css
.glow-cyan {
  box-shadow:
    0 0 20px rgba(0, 212, 255, 0.5),
    0 0 40px rgba(0, 212, 255, 0.3),
    0 0 60px rgba(0, 212, 255, 0.1);
}
```

**Magenta 글로우 (당첨 강조)**
```css
.glow-magenta {
  box-shadow:
    0 0 30px rgba(255, 0, 255, 0.6),
    0 0 60px rgba(255, 0, 255, 0.4),
    0 0 90px rgba(255, 0, 255, 0.2);
}

.text-glow-magenta {
  text-shadow:
    0 0 10px rgba(255, 0, 255, 0.8),
    0 0 20px rgba(255, 0, 255, 0.5),
    0 0 40px rgba(255, 0, 255, 0.3);
}
```

#### 빛기둥 효과

```css
.light-beam {
  background: linear-gradient(
    180deg,
    rgba(0, 255, 204, 0.8) 0%,
    rgba(0, 212, 255, 0.4) 50%,
    transparent 100%
  );
  filter: blur(20px);
}
```

---

### 타이포그래피

#### 폰트 패밀리

```css
/* 한글: Pretendard 또는 Noto Sans KR */
font-family: 'Pretendard', 'Noto Sans KR', -apple-system, sans-serif;

/* 영문/숫자 (당첨 번호): Montserrat 또는 Inter */
font-family: 'Montserrat', 'Inter', sans-serif;
```

#### 폰트 스케일 (대형 스크린용)

| 용도 | 크기 | 굵기 | 클래스 |
|------|------|------|--------|
| 대제목 (Hero) | 120px+ | ExtraBold (800) | `text-[120px] font-extrabold` |
| 당첨 번호 | 200px+ | Black (900) | `text-[200px] font-black` |
| 중제목 | 48-64px | Bold (700) | `text-5xl font-bold` |
| 본문 | 24-32px | Medium (500) | `text-2xl font-medium` |
| 캡션 | 18-20px | Regular (400) | `text-lg` |

#### 텍스트 스타일 예시

```jsx
{/* 대제목 */}
<h1 className="text-[120px] font-extrabold text-white tracking-tight">
  🎉 경품 추첨 🎉
</h1>

{/* 당첨 번호 */}
<span className="text-[200px] font-black text-sfs-cyan
                [text-shadow:0_0_40px_rgba(0,212,255,0.8)]">
  129
</span>

{/* 축하 메시지 */}
<p className="text-5xl font-bold text-sfs-magenta
              [text-shadow:0_0_20px_rgba(255,0,255,0.6)]">
  축하합니다! 🎊
</p>
```

---

### UI 컴포넌트 스타일

#### 정보 박스 (Info Box)

```css
.info-box {
  border: 1px solid rgba(0, 212, 255, 0.3);
  border-radius: 8px;
  background: rgba(11, 16, 38, 0.6);
  backdrop-filter: blur(10px);
  padding: 24px;
}
```

```jsx
<div className="border border-sfs-cyan/30 rounded-lg
                bg-sfs-deep/60 backdrop-blur-md p-6">
  {/* 내용 */}
</div>
```

#### 라벨 태그 (Label Tag)

```css
.label-tag {
  border: 1px solid #00D4FF;
  border-radius: 4px;
  padding: 4px 16px;
  color: #00D4FF;
  font-weight: 500;
  font-size: 14px;
}
```

```jsx
<span className="border border-sfs-cyan rounded px-4 py-1
                 text-sfs-cyan font-medium text-sm">
  행운의 번호
</span>
```

#### CTA 버튼

**Primary (Magenta)**
```jsx
<button className="px-8 py-4 rounded-full font-bold text-xl
                   bg-gradient-to-r from-sfs-magenta to-sfs-pink
                   text-white shadow-lg
                   hover:scale-105 active:scale-95
                   transition-all duration-300
                   [box-shadow:0_0_30px_rgba(255,0,255,0.4)]">
  추첨 시작
</button>
```

**Secondary (Cyan Outline)**
```jsx
<button className="px-8 py-4 rounded-full font-bold text-xl
                   border-2 border-sfs-cyan text-sfs-cyan
                   hover:bg-sfs-cyan/10
                   transition-all duration-300">
  다시 추첨
</button>
```

#### 노드 (네트워크 그래픽용)

```css
.node {
  width: 60px;
  height: 60px;
  border-radius: 50%;
  background: linear-gradient(135deg, #00D4FF, #00FFCC);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: bold;
  color: #0B1026;
  box-shadow: 0 0 20px rgba(0, 212, 255, 0.5);
}

.node.selected {
  width: 100px;
  height: 100px;
  background: linear-gradient(135deg, #FF00FF, #FF66B2);
  box-shadow: 0 0 40px rgba(255, 0, 255, 0.6);
  animation: pulse 1s ease-in-out infinite;
}
```

---

### 애니메이션

#### 펄스 효과 (당첨 강조)

```css
@keyframes pulse {
  0%, 100% {
    transform: scale(1);
    box-shadow: 0 0 40px rgba(255, 0, 255, 0.6);
  }
  50% {
    transform: scale(1.1);
    box-shadow: 0 0 80px rgba(255, 0, 255, 0.8);
  }
}

.animate-pulse-magenta {
  animation: pulse 1s ease-in-out infinite;
}
```

#### 파티클 반짝임

```css
@keyframes twinkle {
  0%, 100% { opacity: 0.3; }
  50% { opacity: 1; }
}

.particle {
  animation: twinkle 2s ease-in-out infinite;
  animation-delay: var(--delay, 0s);
}
```

#### 노드 부유 효과

```css
@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-10px); }
}

.node-float {
  animation: float 3s ease-in-out infinite;
}
```

#### 에너지 흐름 (연결선)

```css
@keyframes energy-flow {
  0% { stroke-dashoffset: 100; }
  100% { stroke-dashoffset: 0; }
}

.edge-animated {
  stroke-dasharray: 10 5;
  animation: energy-flow 1s linear infinite;
}
```

---

## 🎰 추첨 애니메이션 스타일

### Draft 1: 번호 스크롤

**컨셉**: 숫자가 빠르게 스크롤되다가 서서히 감속하며 당첨 번호에서 멈춤

**구현 요소**:
- CSS `transform: translateY()` 기반 스크롤
- easeOutExpo 감속 커브
- 당첨 시 Magenta 하이라이트

### Draft 2: 카드 뒤집기

**컨셉**: 3장의 카드가 순차적으로 뒤집히며 당첨 번호 공개

**구현 요소**:
- CSS 3D `transform: rotateY(180deg)`
- `perspective: 1000px` 깊이감
- 카드 앞면: 물음표 + Cyan 테두리
- 카드 뒷면: 당첨 번호 + Magenta 글로우

### Draft 3: 뉴로 링크 ⭐ (추천)

**컨셉**: 참여자들이 노드로 표시되고, 에너지 파동이 연결선을 타고 흘러 당첨자 선택

**구현 요소**:
- Canvas/SVG 기반 네트워크 그래프
- 노드: 참여자 번호 (Cyan 그라데이션)
- 엣지: 무작위 연결선 (Cyan, 애니메이션)
- 당첨 노드: Magenta 확대 + 폭발 효과

**단계별 애니메이션**:
1. **대기**: 노드들이 부드럽게 떠다님
2. **추첨 시작**: 중앙에서 에너지 파동 발생
3. **선택 과정**: 파동이 엣지를 타고 퍼짐
4. **당첨**: 특정 노드 선택, Magenta 변환, 확대

### Draft 4: RNN 스타일

**컨셉**: AI 신경망처럼 입력 레이어(참여자)에서 출력 레이어(당첨자)로 점점 수렴

**구현 요소**:
- 왼쪽: Input Layer (모든 참여자 번호)
- 중간: Hidden Layers (추상화된 노드들)
- 오른쪽: Output Layer (당첨 번호)
- 레이어 간 활성화 애니메이션

---

## 📐 레이아웃 가이드

### 대형 스크린 (page1, page3)

**해상도**: 1920×1080 (Full HD) 기준, 16:9 가로형
**안전 영역**: 상하좌우 100px 마진

```
┌────────────────────────────────────────────────────────┐
│  margin: 100px                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │                                                  │  │
│  │                  메인 콘텐츠                      │  │
│  │                  (1720 × 880)                    │  │
│  │                                                  │  │
│  └──────────────────────────────────────────────────┘  │
│                                                        │
└────────────────────────────────────────────────────────┘
```

### 모바일 (page2)

**해상도**: 375×812 (iPhone X 기준)
**콘텐츠 영역**: 세로 중앙 정렬

```
┌─────────────┐
│             │
│   상단 로고  │
│             │
│  ─────────  │
│             │
│  행운의 번호 │
│    129      │
│             │
│  ─────────  │
│             │
│   안내 문구  │
│             │
└─────────────┘
```

---

## 🔧 기술 스택

### 프론트엔드

- **프레임워크**: Next.js 16 (App Router)
- **UI 라이브러리**: React 19
- **스타일링**: Tailwind CSS 4.0
- **애니메이션**: CSS + JavaScript (Canvas/SVG)
- **상태 관리**: Context API
- **실시간 통신**: WebSocket (예정)

### 디렉토리 구조 (예정)

```
front/src/app/luckydraw/
├── page.js              # 진입점 (리다이렉트)
├── qr/
│   └── page.js          # page1: QR코드 표시
├── entry/
│   ├── page.js          # page2: 응모 페이지
│   ├── pending/
│   │   └── page.js      # page2-pending: 대기 화면
│   └── win/
│       └── page.js      # page2-win: 당첨 화면
└── draw/
    └── page.js          # page3: 추첨 애니메이션

front/src/components/luckydraw/
├── NetworkGraph.jsx     # 뉴로 링크 네트워크
├── NumberScroller.jsx   # 번호 스크롤러
├── CardFlip.jsx         # 카드 뒤집기
├── RNNVisualization.jsx # RNN 시각화
└── ParticleBackground.jsx # 파티클 배경
```

---

## 📊 데이터 모델

### 참여자 (Participant)

```typescript
interface Participant {
  id: string;              // UUID
  luckyNumber: number;     // 행운의 번호 (001 ~ 999)
  enteredAt: Date;         // 응모 시각
  isWinner: boolean;       // 당첨 여부
  prizeId?: string;        // 당첨 경품 ID
}
```

### 추첨 이벤트 (DrawEvent)

```typescript
interface DrawEvent {
  id: string;              // UUID
  eventId: string;         // SFS2025 이벤트 ID
  prizeId: string;         // 경품 ID
  winnersCount: number;    // 당첨자 수
  status: 'pending' | 'drawing' | 'completed';
  winners: string[];       // 당첨자 ID 목록
  drawnAt?: Date;          // 추첨 완료 시각
}
```

---

## 📈 개발 로드맵

### Phase 1: 기본 구조 (현재)

- [ ] 디자인 가이드 문서화 ✅
- [ ] 프로젝트 구조 세팅
- [ ] 페이지 라우팅 구조 생성
- [ ] 기본 레이아웃 컴포넌트

### Phase 2: 추첨 애니메이션 Draft

- [ ] Draft 1: 번호 스크롤 구현
- [ ] Draft 2: 카드 뒤집기 구현
- [ ] Draft 3: 뉴로 링크 구현
- [ ] Draft 4: RNN 스타일 구현

### Phase 3: 페이지 구현

- [ ] page1: QR코드 표시 화면
- [ ] page2: 응모 페이지
- [ ] page2-pending: 대기 화면
- [ ] page2-win: 당첨 화면
- [ ] page3: 추첨 애니메이션 (선택된 스타일)

### Phase 4: 백엔드 연동

- [ ] WebSocket 실시간 통신
- [ ] 참여자 등록 API
- [ ] 추첨 진행 API
- [ ] 당첨자 알림 시스템

---

## 📞 연락처 및 협업

### 프로젝트 담당자

- **기획**: Product Team
- **디자인**: (SFS2025 디자인팀 협업)
- **프론트엔드**: Development Team
- **백엔드**: Development Team

### 관련 문서

- [프로젝트 CLAUDE.md](../../CLAUDE.md)
- [MVP API 명세서](../api/MVP_API_SPEC.md)
- [Rythmi 케이스 스터디](./RYTHMI.md) - 참고 사례

---

**문서 버전**: 1.0
**최종 업데이트**: 2025-11-26
**작성자**: Development Team
**상태**: Phase 1 진행 중 (디자인 가이드)
