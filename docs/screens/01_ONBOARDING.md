# 온보딩 및 인증 화면

## 목차
- [SCR-001 스플래시 스크린](#scr-001-스플래시-스크린)
- [SCR-002 온보딩 슬라이드](#scr-002-온보딩-슬라이드)
- [SCR-003 회원가입](#scr-003-회원가입)
- [SCR-004 로그인](#scr-004-로그인)

---

## SCR-001 스플래시 스크린

### 기본 정보
- **화면 ID**: SCR-001
- **화면 경로**: `/splash`
- **접근 권한**: 전체 (인증 불필요)
- **표시 시간**: 0.5초 (최소) ~ 2초 (최대)
- **다음 화면**:
  - 신규 사용자 → SCR-002 (온보딩)
  - 기존 사용자 (로그인 상태) → SCR-020 (홈 대시보드)

### 화면 목적
- 앱 초기 로딩 시간 동안 브랜드 노출
- 인증 상태 확인 및 초기 데이터 로드

### 레이아웃

```
┌─────────────────────────────┐
│                             │
│                             │
│                             │
│         [로고]              │
│      eventManager           │
│                             │
│      [태그라인]             │
│   "이벤트를 더 쉽게"         │
│                             │
│                             │
│     [로딩 인디케이터]        │
│                             │
│                             │
└─────────────────────────────┘
```

### UI 컴포넌트

#### 1. 로고
- **위치**: 화면 중앙 (수직 40%)
- **크기**: 120×120px
- **형태**: 아이콘 + 워드마크
- **애니메이션**: Fade in (0.3초)

#### 2. 태그라인
- **텍스트**: "이벤트를 더 쉽게"
- **위치**: 로고 하단 24px
- **스타일**: Body / Regular / 중성 회색
- **애니메이션**: Fade in (0.5초, 0.2초 지연)

#### 3. 로딩 인디케이터
- **위치**: 화면 하단 48px
- **타입**: 스피너 또는 프로그레스 바
- **색상**: Primary
- **크기**: 32×32px

### 백그라운드 로직

```typescript
// 스플래시 화면 로직
async function handleSplash() {
  const startTime = Date.now();

  // 병렬 실행
  const [authStatus, config] = await Promise.all([
    checkAuthStatus(),      // JWT 토큰 유효성 확인
    loadAppConfig(),        // 앱 설정 로드
  ]);

  // 최소 표시 시간 보장 (0.5초)
  const elapsed = Date.now() - startTime;
  if (elapsed < 500) {
    await delay(500 - elapsed);
  }

  // 라우팅 결정
  if (authStatus.isAuthenticated) {
    navigate('/dashboard');
  } else if (config.hasSeenOnboarding) {
    navigate('/login');
  } else {
    navigate('/onboarding');
  }
}
```

### 상태 관리

| 상태 | 설명 | 다음 화면 |
|------|------|----------|
| loading | 초기 로딩 중 | - |
| authenticated | 로그인 상태 확인 | SCR-020 (대시보드) |
| new_user | 신규 사용자 | SCR-002 (온보딩) |
| returning_user | 기존 사용자 (비로그인) | SCR-004 (로그인) |

---

## SCR-002 온보딩 슬라이드

### 기본 정보
- **화면 ID**: SCR-002
- **화면 경로**: `/onboarding`
- **접근 권한**: 신규 사용자
- **슬라이드 수**: 3개
- **다음 화면**: SCR-003 (회원가입)

### 화면 목적
- 제품 핵심 가치 전달
- 주요 기능 소개
- 사용자 기대치 설정

### 레이아웃 (공통)

```
┌─────────────────────────────┐
│  [건너뛰기]          [1/3]  │ ← 헤더
├─────────────────────────────┤
│                             │
│                             │
│       [일러스트]            │ ← 60% 영역
│                             │
│                             │
├─────────────────────────────┤
│     [제목]                  │
│                             │
│     [설명 텍스트]           │ ← 30% 영역
│                             │
│  [●○○]  페이지 인디케이터   │
├─────────────────────────────┤
│      [다음] 버튼            │ ← 10% 영역
└─────────────────────────────┘
```

### 슬라이드 1: 이벤트 생성

#### 콘텐츠
- **일러스트**: 캘린더에 이벤트 추가하는 모습
- **제목**: "3분 만에 이벤트 만들기"
- **설명**: "복잡한 설정 없이 빠르게 이벤트를 생성하고 참여자를 초대하세요"

#### UI 요소
- 페이지 인디케이터: ●○○
- 버튼: "다음" (Primary)

### 슬라이드 2: 출석 체크

#### 콘텐츠
- **일러스트**: 스마트폰으로 출석 체크하는 모습
- **제목**: "현장에서 바로 출석 체크"
- **설명**: "이름 검색 한 번으로 빠른 출석 체크. 오프라인에서도 작동합니다"

#### UI 요소
- 페이지 인디케이터: ○●○
- 버튼: "다음" (Primary)

### 슬라이드 3: 경품 추첨

#### 콘텐츠
- **일러스트**: 경품 추첨 당첨자 발표 화면
- **제목**: "공정한 경품 추첨"
- **설명**: "참여자 중 랜덤으로 당첨자를 선정하고 멋지게 발표하세요"

#### UI 요소
- 페이지 인디케이터: ○○●
- 버튼: "시작하기" (Primary, 크기 더 큼)

### 인터랙션

#### 스와이프 제스처
- **좌→우 스와이프**: 이전 슬라이드
- **우→좌 스와이프**: 다음 슬라이드
- **애니메이션**: 슬라이드 전환 (0.3초, ease-out)

#### 버튼 액션
- **건너뛰기**: SCR-003 (회원가입)으로 즉시 이동
- **다음**: 다음 슬라이드로 이동
- **시작하기** (마지막): SCR-003 (회원가입)으로 이동

#### 자동 재생 (선택사항)
- 5초마다 자동 슬라이드 전환
- 사용자 터치 시 자동 재생 정지

### 데이터
- 로컬 저장: `hasSeenOnboarding: true`
- 서버 전송: 불필요 (로그인 전)

---

## SCR-003 회원가입

### 기본 정보
- **화면 ID**: SCR-003
- **화면 경로**: `/signup`
- **접근 권한**: 비로그인 사용자
- **완료 시간 목표**: 1분 이내
- **다음 화면**: SCR-020 (홈 대시보드)

### 화면 목적
- 신규 사용자 계정 생성
- 기본 프로필 정보 수집
- 빠른 온보딩 완료

### 레이아웃

```
┌─────────────────────────────┐
│  ← [뒤로]        회원가입   │
├─────────────────────────────┤
│                             │
│  환영합니다! 👋              │
│  계정을 만들어 시작하세요    │
│                             │
│  ┌─────────────────────┐   │
│  │ 📧 이메일           │   │
│  └─────────────────────┘   │
│                             │
│  ┌─────────────────────┐   │
│  │ 🔒 비밀번호         │   │
│  └─────────────────────┘   │
│                             │
│  ┌─────────────────────┐   │
│  │ 🔒 비밀번호 확인    │   │
│  └─────────────────────┘   │
│                             │
│  ┌─────────────────────┐   │
│  │ 👤 이름             │   │
│  └─────────────────────┘   │
│                             │
│  ┌─────────────────────┐   │
│  │ 🏢 조직/단체 (선택) │   │
│  └─────────────────────┘   │
│                             │
│  ☑ 이용약관 및 개인정보      │
│     처리방침에 동의합니다    │
│                             │
│  ┌───────────────────────┐ │
│  │   회원가입            │ │ ← Primary Button
│  └───────────────────────┘ │
│                             │
│ ─────── 또는 ───────        │
│                             │
│  [G] Google 계정으로 시작   │
│  [K] 카카오 계정으로 시작   │
│                             │
│  이미 계정이 있으신가요?     │
│  [로그인]                   │
│                             │
└─────────────────────────────┘
```

### UI 컴포넌트

#### 1. 헤더
- **뒤로 버튼**: 온보딩 또는 이전 화면으로
- **제목**: "회원가입" (중앙 정렬)

#### 2. 환영 메시지
- **제목**: "환영합니다! 👋"
- **스타일**: Heading 2 / Bold
- **부제목**: "계정을 만들어 시작하세요"
- **스타일**: Body / Regular / 중성 회색

#### 3. 입력 필드

##### 이메일
- **레이블**: "이메일"
- **플레이스홀더**: "your@email.com"
- **타입**: email
- **검증**: 이메일 형식, 중복 확인
- **아이콘**: 📧 (좌측)

##### 비밀번호
- **레이블**: "비밀번호"
- **플레이스홀더**: "최소 8자 이상"
- **타입**: password
- **검증**: 최소 8자, 영문+숫자 조합
- **아이콘**: 🔒 (좌측), 👁 (우측, 표시/숨김 토글)
- **강도 표시**: 약함/보통/강함 (색상 바)

##### 비밀번호 확인
- **레이블**: "비밀번호 확인"
- **플레이스홀더**: "비밀번호를 다시 입력하세요"
- **타입**: password
- **검증**: 비밀번호와 일치 여부
- **실시간 피드백**: 일치 ✓ / 불일치 ✗

##### 이름
- **레이블**: "이름"
- **플레이스홀더**: "홍길동"
- **타입**: text
- **검증**: 2-20자
- **아이콘**: 👤 (좌측)

##### 조직/단체 (선택)
- **레이블**: "조직/단체 (선택사항)"
- **플레이스홀더**: "소속 조직이나 단체명"
- **타입**: text
- **아이콘**: 🏢 (좌측)

#### 4. 약관 동의
- **체크박스**: 필수 선택
- **텍스트**: "이용약관 및 개인정보 처리방침에 동의합니다"
- **링크**: "이용약관", "개인정보 처리방침" (밑줄, 새 창)

#### 5. 회원가입 버튼
- **텍스트**: "회원가입"
- **스타일**: Primary Button (전체 너비)
- **높이**: 56px
- **상태**:
  - 기본: Primary 색상
  - 비활성화: 회색 (필수 항목 미입력 시)
  - 로딩: 스피너 표시

#### 6. 소셜 로그인
- **구분선**: "또는" 텍스트 포함
- **Google 버튼**:
  - 아이콘: Google 로고
  - 텍스트: "Google 계정으로 시작"
  - 배경: 흰색, 테두리: 회색
- **카카오 버튼**:
  - 아이콘: 카카오 로고
  - 텍스트: "카카오 계정으로 시작"
  - 배경: #FEE500 (카카오 옐로우)

#### 7. 로그인 링크
- **텍스트**: "이미 계정이 있으신가요? [로그인]"
- **스타일**: Body Small / "로그인"은 Primary 색상

### 인터랙션

#### 실시간 검증
```typescript
// 이메일 검증
function validateEmail(email: string) {
  const regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
  if (!regex.test(email)) {
    return { valid: false, message: "올바른 이메일 형식이 아닙니다" };
  }
  // 중복 확인 (디바운스 500ms)
  const isDuplicate = await checkEmailDuplicate(email);
  if (isDuplicate) {
    return { valid: false, message: "이미 사용 중인 이메일입니다" };
  }
  return { valid: true };
}

// 비밀번호 강도 평가
function evaluatePasswordStrength(password: string) {
  let strength = 0;
  if (password.length >= 8) strength++;
  if (/[a-z]/.test(password)) strength++;
  if (/[A-Z]/.test(password)) strength++;
  if (/[0-9]/.test(password)) strength++;
  if (/[^a-zA-Z0-9]/.test(password)) strength++;

  return {
    level: strength < 2 ? 'weak' : strength < 4 ? 'medium' : 'strong',
    color: strength < 2 ? 'error' : strength < 4 ? 'warning' : 'success'
  };
}
```

#### 버튼 액션
```typescript
async function handleSignup() {
  // 1. 최종 검증
  const errors = validateForm();
  if (errors.length > 0) {
    showErrors(errors);
    return;
  }

  // 2. 로딩 시작
  setLoading(true);

  try {
    // 3. 회원가입 API 호출
    const response = await api.post('/auth/signup', {
      email: formData.email,
      password: formData.password,
      profile: {
        name: formData.name,
        organization: formData.organization
      }
    });

    // 4. 토큰 저장
    localStorage.setItem('accessToken', response.data.accessToken);
    localStorage.setItem('refreshToken', response.data.refreshToken);

    // 5. 성공 메시지
    toast.success('환영합니다! 🎉');

    // 6. 대시보드로 이동
    navigate('/dashboard');

  } catch (error) {
    // 에러 처리
    if (error.code === 'EMAIL_DUPLICATE') {
      setFieldError('email', '이미 사용 중인 이메일입니다');
    } else {
      toast.error('회원가입에 실패했습니다. 다시 시도해주세요.');
    }
  } finally {
    setLoading(false);
  }
}
```

#### 소셜 로그인
```typescript
async function handleSocialLogin(provider: 'google' | 'kakao') {
  // OAuth 플로우 시작
  const authUrl = await api.get(`/auth/${provider}/url`);

  // 팝업 또는 리다이렉트
  window.location.href = authUrl.data.url;

  // 콜백 처리는 /auth/callback 라우트에서
}
```

### 데이터

#### API 엔드포인트
```
POST /api/auth/signup
Content-Type: application/json

Request:
{
  "email": "user@example.com",
  "password": "SecurePass123!",
  "profile": {
    "name": "홍길동",
    "organization": "북클럽"
  }
}

Response (200):
{
  "user": {
    "id": "uuid",
    "email": "user@example.com",
    "profile": {
      "name": "홍길동",
      "organization": "북클럽"
    },
    "tier": "free",
    "created_at": "2025-10-12T10:00:00Z"
  },
  "accessToken": "eyJhbG...",
  "refreshToken": "eyJhbG..."
}

Response (400 - 검증 실패):
{
  "error": "VALIDATION_ERROR",
  "message": "이메일 형식이 올바르지 않습니다",
  "field": "email"
}

Response (409 - 중복 이메일):
{
  "error": "EMAIL_DUPLICATE",
  "message": "이미 사용 중인 이메일입니다"
}
```

### 상태 관리

| 상태 | 설명 | UI 반영 |
|------|------|---------|
| idle | 초기 상태 | 버튼 활성화 (입력 완료 시) |
| validating | 실시간 검증 중 | 필드 로딩 스피너 |
| submitting | 회원가입 진행 | 버튼 로딩 스피너 |
| success | 회원가입 성공 | 대시보드로 리다이렉트 |
| error | 회원가입 실패 | 에러 메시지 표시 |

### 에러 처리

| 에러 타입 | 메시지 | 액션 |
|----------|--------|------|
| 이메일 형식 오류 | "올바른 이메일 형식이 아닙니다" | 필드 하단에 빨간색 표시 |
| 이메일 중복 | "이미 사용 중인 이메일입니다" | 필드 하단에 빨간색 표시 |
| 비밀번호 약함 | "최소 8자 이상, 영문+숫자 조합" | 필드 하단에 노란색 표시 |
| 비밀번호 불일치 | "비밀번호가 일치하지 않습니다" | 필드 하단에 빨간색 표시 |
| 약관 미동의 | "이용약관에 동의해주세요" | 체크박스 흔들림 애니메이션 |
| 네트워크 오류 | "네트워크 오류가 발생했습니다" | Toast 메시지 + 재시도 버튼 |
| 서버 오류 | "일시적인 오류입니다. 잠시 후 다시 시도해주세요" | Toast 메시지 |

---

## SCR-004 로그인

### 기본 정보
- **화면 ID**: SCR-004
- **화면 경로**: `/login`
- **접근 권한**: 비로그인 사용자
- **다음 화면**: SCR-020 (홈 대시보드)

### 화면 목적
- 기존 사용자 인증
- 세션 생성
- 빠른 로그인 경험

### 레이아웃

```
┌─────────────────────────────┐
│  ← [뒤로]         로그인    │
├─────────────────────────────┤
│                             │
│  다시 오신 것을 환영합니다!  │
│                             │
│  ┌─────────────────────┐   │
│  │ 📧 이메일           │   │
│  └─────────────────────┘   │
│                             │
│  ┌─────────────────────┐   │
│  │ 🔒 비밀번호         │   │
│  └─────────────────────┘   │
│                             │
│  ☑ 로그인 상태 유지         │
│                             │
│           [비밀번호 찾기]    │
│                             │
│  ┌───────────────────────┐ │
│  │      로그인           │ │
│  └───────────────────────┘ │
│                             │
│ ─────── 또는 ───────        │
│                             │
│  [G] Google 계정으로 로그인 │
│  [K] 카카오 계정으로 로그인 │
│                             │
│  아직 계정이 없으신가요?     │
│  [회원가입]                 │
│                             │
└─────────────────────────────┘
```

### UI 컴포넌트

#### 1. 헤더
- **제목**: "로그인"
- **뒤로 버튼**: 이전 화면으로

#### 2. 환영 메시지
- **텍스트**: "다시 오신 것을 환영합니다!"
- **스타일**: Heading 2 / Bold

#### 3. 입력 필드

##### 이메일
- **플레이스홀더**: "your@email.com"
- **타입**: email
- **자동완성**: 활성화
- **저장된 이메일**: 자동 입력 (선택사항)

##### 비밀번호
- **플레이스홀더**: "비밀번호"
- **타입**: password
- **표시/숨김 토글**: 👁 아이콘

#### 4. 옵션

##### 로그인 상태 유지
- **타입**: 체크박스
- **기본값**: 선택됨
- **설명**: "30일간 로그인 유지"

##### 비밀번호 찾기
- **타입**: 링크
- **스타일**: Body Small / Primary 색상
- **액션**: 비밀번호 재설정 플로우

#### 5. 로그인 버튼
- **텍스트**: "로그인"
- **스타일**: Primary Button (전체 너비)
- **높이**: 56px
- **단축키**: Enter 키

#### 6. 소셜 로그인
- Google, 카카오 (회원가입과 동일)

#### 7. 회원가입 링크
- **텍스트**: "아직 계정이 없으신가요? [회원가입]"

### 인터랙션

#### 로그인 처리
```typescript
async function handleLogin() {
  setLoading(true);

  try {
    const response = await api.post('/auth/login', {
      email: formData.email,
      password: formData.password,
      rememberMe: formData.rememberMe
    });

    // 토큰 저장
    const storage = formData.rememberMe ? localStorage : sessionStorage;
    storage.setItem('accessToken', response.data.accessToken);
    storage.setItem('refreshToken', response.data.refreshToken);

    // 사용자 정보 저장
    setUser(response.data.user);

    // 성공 메시지
    toast.success(`환영합니다, ${response.data.user.profile.name}님!`);

    // 리다이렉트
    const returnUrl = getReturnUrl() || '/dashboard';
    navigate(returnUrl);

  } catch (error) {
    if (error.code === 'INVALID_CREDENTIALS') {
      setError('이메일 또는 비밀번호가 올바르지 않습니다');
      // 입력 필드 흔들림 애니메이션
      shakeInputs();
    } else {
      toast.error('로그인에 실패했습니다');
    }
  } finally {
    setLoading(false);
  }
}
```

#### 자동 로그인
```typescript
// 앱 시작 시 토큰 확인
async function checkAutoLogin() {
  const token = localStorage.getItem('accessToken') ||
                sessionStorage.getItem('accessToken');

  if (!token) return false;

  try {
    // 토큰 유효성 검증
    const response = await api.get('/auth/me', {
      headers: { Authorization: `Bearer ${token}` }
    });

    setUser(response.data);
    return true;

  } catch (error) {
    // 토큰 만료 또는 무효
    clearTokens();
    return false;
  }
}
```

### 데이터

#### API 엔드포인트
```
POST /api/auth/login
Content-Type: application/json

Request:
{
  "email": "user@example.com",
  "password": "SecurePass123!",
  "rememberMe": true
}

Response (200):
{
  "user": {
    "id": "uuid",
    "email": "user@example.com",
    "profile": {
      "name": "홍길동",
      "organization": "북클럽"
    },
    "tier": "free"
  },
  "accessToken": "eyJhbG...",
  "refreshToken": "eyJhbG..."
}

Response (401 - 인증 실패):
{
  "error": "INVALID_CREDENTIALS",
  "message": "이메일 또는 비밀번호가 올바르지 않습니다"
}
```

### 에러 처리

| 에러 | 메시지 | UI 반영 |
|------|--------|---------|
| 잘못된 인증 정보 | "이메일 또는 비밀번호가 올바르지 않습니다" | 입력 필드 빨간색 테두리 + 흔들림 |
| 계정 잠김 | "보안을 위해 계정이 일시 잠겼습니다. 30분 후 다시 시도해주세요" | 모달 표시 |
| 네트워크 오류 | "네트워크 연결을 확인해주세요" | Toast 메시지 |

---

**문서 버전:** 1.0
**최종 업데이트:** 2025-10-12
**다음 문서:** [02_EVENT_CREATION.md](./02_EVENT_CREATION.md)
